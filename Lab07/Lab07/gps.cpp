/************************************************
 * Student Name: Milind Paliath-Pathiyal
 * Student Number: 20714398
 * SYDE 121 Lab
 *
 * I hereby declare that this code, submitted
 * for credit for the course SYDE121, is a product
 * of my own efforts. This coded solution has
 * not been plagiarized from other sources and
 * as not been knowingly plagiarized by others.
 */
/*
#include <iostream>
#include "coord.h"
using namespace std;

void getPoint (Point3D &point);
void printPoint (const Point3D &point);
void spatialDist(const Point3D &pointA,const Point3D &pointB);
void planiDist(const Point3D &pointA, const Point3D &pointB);
void htDiff(const Point3D &pointA, const Point3D &pointB);
void azimuth(const Point3D &pointA, const Point3D &pointB);

int main(){
    
    
    Point3D point1;
    Point3D point2;
    
    cout << "Enter the points for point 1: " << endl;
    getPoint(point1);
    
    cout << "Enter the points for point 2: " << endl;
    getPoint(point2);
    
    cout <<"The points for Point 1 are: " << endl;
    printPoint(point1);
    
    cout <<"The points for Point 2 are: " << endl;
    printPoint(point2);
    
    cout << "The spatial distance between the two points is: ";
    spatialDist(point1, point2);
    
    cout << endl << "The planar distance bewteen the two points is: ";
    planiDist(point1, point2);
    
    cout << endl << "The height difference between the two points is: ";
    htDiff(point1, point2);
    
    cout << endl << "The azimuth between the two points is: ";
    azimuth(point1, point2);
    
}

*/
